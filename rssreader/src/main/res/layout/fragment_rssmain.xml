<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context=".MainActivity" android:paddingLeft="5dp" android:paddingRight="5dp">

    <ListView
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:divider="@android:color/transparent"
            android:dividerHeight="10.0sp"
            android:id="@+id/listView" android:layout_centerHorizontal="true"
            tools:listitem="@layout/post_no_image_layout" tools:listheader="@layout/post_no_image_layout"
            tools:listfooter="@layout/post_no_image_layout"/>
</RelativeLayout>
